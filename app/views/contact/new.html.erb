<h1>Contact us</h1>

<!-- form_tag is a rails bult-in helper to generate a form, the first argument
      of the form_tag must be the URL that the form will submit to. Generated
      form will have POST as their method and they will automatically generate
      auth token-->
<%= form_tag contact_submit_path do%>
  <%# will generate a submit button for the form with label 'Contact Us' %>
  <div class="">
    <%= label_tag :name %>
    <%= text_field_tag :name %>
  </div>
  <div class="">
    <%= label_tag :email %>
    <%= email_field_tag :email %>
  </div>
  <div class="">
    <%= label_tag :message %>
    <%= text_area_tag :message %>
  </div>
  <%= submit_tag 'Contact Us' %>
<% end %>
